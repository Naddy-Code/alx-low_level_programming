#include <stdio.h>
/**
 * main - A progam that prints a line with put function
 * Return: Always 0 (success)
 */
int main(void)
{
puts("\"Progamming is like building a multilingual puzzle");
return (0);
}
